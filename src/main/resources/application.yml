# Налаштування для Spring Security
spring:
  security:
    user:
      name: user
      password: default
      roles: USER

      # application.yml

      spring:
        datasource:
          url: jdbc:h2:mem:testdb
          driver-class-name: org.h2.Driver
          username: sa
          password:
        h2:
          console:
            enabled: true
        flyway:
          enabled: true

      # Додайте налаштування для Spring Security
      security:
        user:
          name: user
          password: jdbcDefault